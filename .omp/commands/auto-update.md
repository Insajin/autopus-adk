---
description: 하네스 업데이트 — 현재 repo 또는 메타 workspace를 감지해 하네스 surface를 갱신합니다
agent: build
---

`$ARGUMENTS`

Treat the text above as the full argument payload for `/auto-update`.
Load exact detail skill `auto-update`; this is the same target selected by `/auto update ...`.
Preserve `--model <provider/model>` and `--variant <value>` when present.
Do not restate or expand the arguments unless needed for execution.
