---
name: auto-map
description: 코드베이스 구조, entrypoint, 의존성, ownership을 분석합니다
platform: antigravity-cli
---

# auto-map

## Context Profile

Load core workspace policy and architecture evidence. Load only files needed for the requested scope.

## Contract

1. Resolve the requested repository/module boundary before scanning.
2. Delegate independent read-only discovery when available.
3. Summarize directories, entrypoints, dependencies, public APIs, generated boundaries, and notable risks with file references.
4. Do not modify code or project documents unless the user separately requests a change.
