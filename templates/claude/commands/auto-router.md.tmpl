---
name: auto
description: Autopus-ADK thin router — 하나의 상세 auto workflow만 선택합니다
---

# /auto — Thin Router

> 🐙 Autopus | {{.ProjectName}} | {{.Mode}}

ARGUMENTS: $ARGUMENTS

## Language Policy

- AI responses follow the configured project language policy. Code comments and commit messages retain their configured languages.

## Source Ownership

- Read `.autopus/project/workspace.md` first when present. It defines the root role, nested git repo boundaries, generated/runtime paths, tracking/commit policy, source-of-truth locations, and QA target routing such as `auto qa init --project-dir <repo>`.
- Resolve a SPEC as `.autopus/specs/{SPEC-ID}/spec.md`, then recursively under module roots while excluding generated and dependency directories. Stop on zero or duplicate matches.

## Subagent Delegation

- Preserve the harness subagent-first contract. `--auto` authorizes the configured pipeline; `--solo` explicitly selects a single session.
- If runtime policy prevents implicit delegation, report the constraint and request subagent opt-in or `--solo`; never silently downgrade.

## Review Convergence

- Separate finding discovery from verification. Retries verify only open findings against the new diff and keep configured retry limits.

## Generated Surface Safety

- Do not directly edit generated/runtime roots such as `.claude/**`, `.codex/**`, `.gemini/**`, `.opencode/**`, or `.autopus/plugins/**`.
- Change the canonical ADK template/source, regenerate, and exclude generated/runtime drift from commits.

## Routing Contract

1. Strip global flags while preserving their values: `--auto`, `--loop`, `--multi`, `--quality`, `--team`, `--solo`, `--model`, `--variant`, `--think`, `--ultrathink`, `--workflow`.
2. Use the first remaining token as the route. For natural-language input, perform bounded triage and select one route; without `--auto`, confirm a material route change.
3. Load exactly one detail from the table below. Do not preload other auto details or the generation-only workflow source.
4. Pass all arguments and flags unchanged to the selected detail.

| Route | Load exactly this detail |
|---|---|
| `setup` | `.claude/skills/auto-setup/SKILL.md` |
| `status` | `.claude/skills/auto-status/SKILL.md` |
| `goal` | `.claude/skills/auto-goal/SKILL.md` |
| `update` | `.claude/skills/auto-update/SKILL.md` |
| `plan` | `.claude/skills/auto-plan/SKILL.md` |
| `go` | `.claude/skills/auto-go/SKILL.md` |
| `fix` | `.claude/skills/auto-fix/SKILL.md` |
| `review` | `.claude/skills/auto-review/SKILL.md` |
| `sync` | `.claude/skills/auto-sync/SKILL.md` |
| `idea` | `.claude/skills/auto-idea/SKILL.md` |
| `map` | `.claude/skills/auto-map/SKILL.md` |
| `why` | `.claude/skills/auto-why/SKILL.md` |
| `verify` | `.claude/skills/auto-verify/SKILL.md` |
| `secure` | `.claude/skills/auto-secure/SKILL.md` |
| `test` | `.claude/skills/auto-test/SKILL.md` |
| `qa` | `.claude/skills/auto-qa/SKILL.md` |
| `dev` | `.claude/skills/auto-dev/SKILL.md` |
| `canary` | `.claude/skills/auto-canary/SKILL.md` |
| `doctor` | `.claude/skills/auto-doctor/SKILL.md` |

## Preserved Aliases

- `browse` loads `.claude/skills/browser-automation/SKILL.md`.
- `stale` and `spec review` route through `review` with the alias arguments intact.
- Management aliases `init` and `platform` route through `update`; `doctor` uses its dedicated route.
- Empty input prints the route and alias inventory. Unknown input reports the nearest route without loading multiple details.

## Context Selection

- Every route starts with core workspace policy only: `AGENTS.md`/`CLAUDE.md` and `.autopus/project/workspace.md` when present.
- The selected detail declares its additional context profile. Do not load scenarios, canary, signatures, learnings, or unrelated product documents unless that detail requests them.

Begin each response with `🐙 Autopus ─────────────────────────` and end the selected workflow response with `🐙`.
