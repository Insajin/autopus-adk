// GENERATED by auto generate-templates from content/workflows/route_a.{md,schema.json} — DO NOT EDIT. Edit the manifest SoT instead.
//
// route-a deterministic workflow adapter. The manifest (route_a.md +
// route_a.schema.json) is the source of truth; this file is regenerated.

export const meta = {
  name: 'route-a',
  description: 'Deterministic opt-in Route A workflow (planning -> implementation -> gate -> release hygiene).',
  phases: [
    {title:'planning'},
    {title:'implementation'},
    {title:'gate_build_test'},
    {title:'release_hygiene'},
  ],
};

// Workflow API globals: agent, phase, log, args.
const SEGMENT = (args && args.segment) || 'A';

if (SEGMENT === 'A') {
await phase('planning');
// route_a baseline planning: retry: 0, budget: 60000
log('phase', 'planning');

await phase('implementation');
// route_a baseline implementation: retry: 0, budget: 120000
log('phase', 'implementation');

await phase('gate_build_test');
// route_a baseline gate_build_test: retry: 0, budget: 40000
// Deterministic gate: verdict_source 'exit_code'.
// This gate is executed outside the JS by the Go runtime.
log('gate', 'gate_build_test');

}

if (SEGMENT === 'B') {
await phase('release_hygiene');
// route_a baseline release_hygiene: retry: 0, budget: 30000
// Release hygiene: generated/runtime drift + staged source size gate.
// This gate is executed outside the JS by the Go runtime.
log('release_hygiene', 'release_hygiene');

}
